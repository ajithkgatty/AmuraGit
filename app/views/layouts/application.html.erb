<!DOCTYPE html>
<html>
  <head>
    <title>GitSample</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<div class="container">
  		<% if !current_user.blank? %>
  			 <div class="navbar-text navbar-right">Hello <%= current_user.username %>, <%= link_to "Sign Out", signout_path, method: :delete %></a>
 			 </div>
  		<% end %>
    <%= yield %>
	</div>
  </body>
</html>
